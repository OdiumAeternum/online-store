const Router = require('express')
const router = new Router()

//для того, чтобы создать девайс
router.post('/',)
//для того, чтобы получать девайс
router.get('/',)
//получить отдельно конкретный девайс
//после того, как перешли на страницу полной информации
router.get('/:id',)

module.exports = router